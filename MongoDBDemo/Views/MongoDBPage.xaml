<Page x:Class="MongoDBDemo.Pages.MongoDBPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MongoDBDemo.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="400"
      Title="MongoDBPage">

    <StackPanel>
        <Button Content="Click to get database names"
                Command="{Binding LogDatabaseNamesCommand}"/>

        <ListBox ItemsSource="{Binding DatabaseNames}" Height="400">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Margin="0 0 5 0"/>
                        <TextBlock Text="{Binding SizeOnDisk}" Margin="0 0 5 0"/>
                        <TextBlock Text="{Binding IsEmpty}" Margin="0 0 5 0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Page>
